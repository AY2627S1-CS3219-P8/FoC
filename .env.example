# ─────────────────────────────────────────────────────────────
# FoC — Friend on Campus · Environment configuration (EXAMPLE)
# ─────────────────────────────────────────────────────────────
# Copy to `.env` and fill in real values:   cp .env.example .env
#
# `.env` is git-ignored — NEVER commit real secrets.
# Any variable read by any service MUST appear here with a safe
# placeholder so teammates know what to set.
#
# This is just an example; you can add more variables as needed.
# ─────────────────────────────────────────────────────────────
# ---- Global -------------------------------------------------
LOG_LEVEL=
# ---- Auth / JWT ---------------------------------------------
JWT_SECRET=
JWT_ACCESS_TOKEN_TTL=
JWT_REFRESH_TOKEN_TTL=
# ---- User Database ------------------------------------------
POSTGRES_USER=foc
# Single-quote passwords so Compose preserves `$`, spaces, and `#` literally.
POSTGRES_PASSWORD='change-me-locally'
# ---- Optional Order Service integration --------------------
# Leave ORDER_SERVICE_URL empty until Order Service is deployed. In that
# state owner profiles return order_history_status=unavailable.
ORDER_SERVICE_URL=
ORDER_SERVICE_TOKEN=
ORDER_SERVICE_TIMEOUT_SECONDS=2
